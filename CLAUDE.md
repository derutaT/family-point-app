# 家族ポイントアプリ

## 概要
子供の遊び続けたいとやらないといけない事のバランスを取るためにポイント制を導入します。
ゲームや動画視聴など嗜好性が強く依存性が高いあそびについてはポイントを消費します。
お手伝いや宿題などの努力を必要とする行為にはポイントを付与します。
このポイントでバランスの取れた日常を形成する事を目的としています。

# 構成
単一の HTML ファイル（`family-point-app.html`）で完結している。

## 技術スタック
- バニラ HTML / CSS / JavaScript（フレームワークなし）
- データ永続化: `localStorage`（キー: `family-point-app`）

## アーキテクチャ
- **状態管理**: グローバル変数 `state`（`loadState` / `saveState` で localStorage と同期）
- **レンダリング**: `render()` が毎回 `app.innerHTML = ''` してフル再描画
- **DOM生成**: ユーティリティ関数 `el(tag, attrs, ...children)` で構築（仮想DOMなし）
- **スタイル**: CSS変数（`:root`）＋スタイルオブジェクト定数 `S`。ダークモード対応（`prefers-color-scheme`）

# 主要機能
- 親と子の2ロール
- 親ユーザーはPIN認証が必要
- 子ユーザーは特に認証は不要で自分の名前を選ぶ
- 毎朝5時に日替わりポイントを付与（デフォルト60pt）
- 子供はタスクをこなすことでポイントが付与される
  - タスクには親の承認が必要なタスクと、ボタンを押しただけで即時反映されるポイントがある
- ポイントを利用して遊びを開始できる
- タイマー機能
  - 遊びの終了を教えるためのアラート機能がある
- ポイントの種類
  - 日替わりポイント、タスクポイント、の2種類ある
  - ポイント消費順: 日替わりポイント → タスクポイント

# 開発

## ドキュメント構造

```
CLAUDE.md # Claude Code エントリーポイント
TASKS.md # Project の TASK を管理
docs/
  spec/ # 現在の仕様（シンプルにまとめる）
  adr/ # ADR
  task_plan/ # TASK の計画、TASK に着手したタイミングで作成して、進め方を決めて書き出す
```

## 開発ルール
- 変更後は必ずブラウザで動作確認する（ビルドステップなし）。
- UI テキストは日本語で統一する。
